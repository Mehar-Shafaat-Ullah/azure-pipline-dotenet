trigger:
- main

pool:
  name: local-agnet # Ensure this matches your self-hosted agent pool

steps:
- script: echo "Hello from Azure Pipeline!"
  displayName: "Greeting (CMD)"
  # No 'shell:' property needed; it defaults to cmd.exe on Windows.
  
- script: |
    echo Checking content of index.html
    type index.html
  displayName: "Show index.html content (CMD)"
  # 'type' is the native CMD command to display file contents.
